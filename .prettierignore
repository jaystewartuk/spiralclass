# Generated by scripts/generate-erd-schema.mjs via `JSON.stringify(graph, null, 2)`.
# A drift-guard test (apps/web/tests/erd-schema.test.ts) compares this file
# byte-for-byte against fresh generator output, which never packs short
# arrays onto one line the way Prettier's JSON printer does — so running
# Prettier on this file breaks that comparison. Never format it.
apps/web/src/lib/erd/schema.generated.json

# Generated by pnpm. Prettier's YAML printer rewrites its quoting (single to
# double), which produces a ~22,900-line diff nobody asked for on any run of
# `pnpm format` — and the diff is invisible to the gate, because
# scripts/format-check.mjs skips lockfiles before it ever reaches Prettier. So
# the churn lands in a commit and only shows up in review. Never format it.
pnpm-lock.yaml
