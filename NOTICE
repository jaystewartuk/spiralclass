# Third-party notices

SpiralClass is licensed under the GNU Affero General Public License v3.0 only
(see `LICENSE`). This file records third-party material redistributed **inside
this repository's own source tree** — material whose license requires its
copyright notice and license to travel with it.

Dependencies resolved at install time from `package.json` are not listed here;
their licenses ship in `node_modules` and are not redistributed by this
repository.

---

## Atkinson Hyperlegible

Copyright 2020 Braille Institute of America, Inc.

Licensed under the SIL Open Font License, Version 1.1. The full license text is
available at <https://openfontlicense.org> and ships with the upstream package
(`node_modules/@fontsource/atkinson-hyperlegible/LICENSE`).

**Where it is redistributed here:** `apps/web/src/lib/og-font-data.ts` embeds
the latin subset of the regular and bold faces as base64, generated by
`apps/web/scripts/og-font.mjs` from `@fontsource/atkinson-hyperlegible`. The
font is inlined rather than read from disk because Satori requires a font and
`output: standalone` does not ship `node_modules` — see that script's header.

The Reserved Font Name provisions of OFL 1.1 apply: this repository does not
modify the font, and any derivative must not be distributed under the name
"Atkinson Hyperlegible".

---

## shadcn/ui

Copyright (c) 2023 shadcn

Licensed under the MIT License. The full text ships with the upstream project
at `shadcn-ui/ui`, file `LICENSE.md`.

**Where it is redistributed here:** components generated by the `shadcn` CLI
into `apps/web/src/components/ui/`, and the `cn()` helper in
`apps/web/src/lib/utils.ts`. `apps/web/components.json` is that CLI's config.
The set is `alert`, `badge`, `button`, `card`, `checkbox`, `dialog`, `input`,
`label`, `pagination`, `select`, `sheet`, `skeleton`, `tabs`, `textarea` and
`tooltip`; everything else in that folder is this project's own. All fifteen
have been modified — the class strings carry this project's tokens rather than
the generator's defaults.

shadcn/ui distributes source rather than a package, so these files are not
resolved at install time and the notice cannot travel in `node_modules`. It
travels here instead. The Radix primitives several of them wrap are ordinary
dependencies and are not redistributed by this repository.
