# Template only — no real values. Names must match infra/infisical/README.md's
# "Vars Infisical owns" list exactly; see that file's "Where to get each
# value" section for where each one comes from. Fill in and import into the
# Infisical "preview" environment (dashboard bulk-paste, or
# `infisical secrets set --file=...` if the CLI supports it — check
# `infisical secrets set --help`, not verified in this session).
#
# Never commit this file with real values filled in.

# Neon (preview project — see infra/database/neon/README.md; Supabase was
# decommissioned D-89, its SUPABASE_SERVICE_ROLE_KEY/SUPABASE_JWT_SECRET no
# longer read by anything)
DATABASE_URL=
DIRECT_URL=

# Self-generated (openssl rand -base64 32, or your own value for the Maestro one)
SESSION_SECRET=
BETTER_AUTH_SECRET=
FIELD_ENCRYPTION_KEY=

# Stripe (test-mode keys for preview)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_BILLING_WEBHOOK_SECRET=

# Single-vendor API keys
ANTHROPIC_API_KEY=
ELEVENLABS_API_KEY=
GOOGLE_TTS_API_KEY=
RESEND_API_KEY=
SES_ACCESS_KEY_ID=
SES_SECRET_ACCESS_KEY=
DEEPGRAM_API_KEY=
ASSEMBLYAI_API_KEY=

# Inngest
INNGEST_EVENT_KEY=
INNGEST_SIGNING_KEY=

# PostHog
POSTHOG_PERSONAL_API_KEY=

# Google Cloud Console (two separate OAuth clients — don't mix up)
GOOGLE_OAUTH_CLIENT_SECRET=
GOOGLE_CLIENT_SECRET=

# Azure
AZURE_SPEECH_KEY=

# LiveKit
LIVEKIT_API_SECRET=

# Web Push (VAPID) — self-generated, see the README's "Where to get each
# value". A DIFFERENT keypair per environment, and never rotated once live.
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=
