name: Deploy production

# Production deploys when `pnpm promote` fast-forwards the `production` branch
# ([D-157]), after a required reviewer approves the run.
#
# ⚠️ This workflow does not deploy anything, and it does not gate anything
# either. `scripts/fly-deploy.sh` deploys; `pnpm promote` gates. Both already
# existed and neither is re-typed here.
#
# THE GATE STILL RUNS ON THE LAPTOP, AND STILL RUNS FIRST. `production` is a
# fast-forward-only branch that only `pnpm promote` moves, and promote runs the
# FULL tier (mutation · integration · E2E) against the exact commit before it
# pushes. So by the time this workflow starts, the commit it is deploying has
# been certified. That ordering is why `--gate-already-passed` is honest here:
# it is a handoff, not a bypass.
#
# ⚠️ The full tier still runs LOCALLY before this fires, and after [D-161] that
# is no longer because it has to. The blocker D-157 named — darwin-only visual
# baselines — is gone, and heavy.yml runs the same suites on a runner for every
# PR. What keeps the promote gate local is the RECEIPT: `pnpm promote` reuses a
# green full-tier receipt bound to this exact commit, and a runner's filesystem
# is thrown away. Certifying a release from a runner is a separate decision.
#
# THE HUMAN CHECKPOINT, AND WHY IT IS NOT THE 2026-07-19 DRIFT AGAIN.
# D-120's binding lesson was that a HUMAN DISPATCH STEP left `production` 96
# commits behind what was live, so the deploy was chained into `pnpm promote`
# and a guard test asserted it stayed chained. This moves the checkpoint into
# the platform instead — a required reviewer on the `production` environment —
# and the difference is that the two failures do not look alike:
#
#   * A forgotten dispatch is INVISIBLE. Nothing anywhere says the deploy is
#     owed. That is how 96 commits accumulated.
#   * A pending environment approval is a queued run sitting in the Actions
#     tab with a notification attached, and `pnpm promote` in the foreground
#     waiting on it.
#
# ⚠️ `pnpm release:status` is NOT the third witness here, and an earlier
# version of this comment claimed it was. Its production-web row reads the
# LOCAL ledger, which a runner deploy cannot write (D-157's unresolved risks) —
# so it is either silent about a deploy that happened or, worse, still holding
# the last laptop deploy's SHA and reporting production as behind when it is
# current. The authoritative answers are `git log origin/production`, the Fly
# release list, and this workflow's run history.
#
# The checkpoint is worth having because this is the one deploy that runs
# migrations against live payment records behind a Neon restore point. It is
# the last look before that.
#
# ⚠️ SUSPENDED 2026-09-04 AND UNSUSPENDED THE SAME DAY. Read the two decisions
# together or this file looks like it changed its mind for no reason.
#
# The suspension came from "there are no more Fly deploys" — everything was
# moving to the Oracle A1 box (D-150). Hours later that was narrowed: **the
# production web app stays on Fly and production's database stays on Neon;
# Oracle carries preview, preview's database and the LiveKit stack** (D-150's
# second addendum). Fly is production's target, not a target being retired, so
# there is nothing here to suspend — and a `production` branch that cannot
# deploy is the 2026-07-19 drift waiting to happen.
#
# ⚠️ Preview's workflow is still suspended, and for a reason that does not
# apply here: its target genuinely is moving, and the box is not serving yet.
#
# WHERE THE VALUES COME FROM ([D-163], and read its addendum before changing
# this). Infisical is the source of truth, and it PUSHES a copy into this
# environment's secrets — the same direction `infra/infisical/push-fly-secrets.sh`
# already pushes the app's runtime secrets into Fly. One vault, two push
# targets, one mental model. Rotating a value in Infisical re-syncs it here, so
# the copy below is derived rather than a second original.
#
# ⚠️ THE JOB DELIBERATELY HOLDS NO CREDENTIAL THAT CAN FETCH ANYTHING ELSE, and
# that is the whole reason this is a push rather than a pull. D-163 first
# shipped as an OIDC machine identity reading Infisical at run time, which is
# tidier on paper and worse here: `id-token: write` is granted per JOB, so
# GitHub puts the token-minting variables in the environment of EVERY step —
# including `pnpm install`, which runs the lifecycle scripts of the whole
# dependency tree, and the Docker build. Anything running there could mint its
# own OIDC token, authenticate as the identity, and read whatever that identity
# could read, no matter how narrow this workflow's own request was.
#
# With the values pushed, a compromised dependency gets these twelve and has no
# way to ask for a thirteenth. The blast radius is bounded by what is here rather
# than by an access policy somewhere else. `apps/web/tests/config/local-gate.test.ts`
# asserts the `id-token` permission stays absent.
#
# The `config/env/*.local.env` overlays are still gone, and that half of D-163
# stands: the laptop reads the same values through
# `infra/infisical/run.sh production ./scripts/fly-deploy.sh …` rather than from
# a file that is the copy nobody rotates.
#
# THE PREFLIGHT, AND WHY IT DID NOT GO AWAY WITH THEM. Every step below is the
# same script the operator runs by hand, and it discovers a missing value where
# it is first used: after the Neon checkpoint for the database URLs, after the
# migrations for the Fly token. A fetch that quietly returned fewer secrets than
# expected looks exactly like a fetch that worked. So the step after the fetch
# proves the whole set before anything is touched. It checks; it never
# re-implements.
#
# `LIVEKIT_ORIGIN_IP` is in that set for a different reason. It belongs to the
# probe step at the END, which since D-134 refuses to run without it — so
# without this check a correct deploy would finish and then go red, at the one
# moment nobody should have to work out whether production shipped.

on:
  push:
    branches: [production]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: deploy-production
  # ⚠️ Never true. Cancelling this run can leave migrations applied with no
  # image deployed against them — production serving old code against a new
  # schema. Queue instead.
  cancel-in-progress: false

jobs:
  deploy:
    name: Checkpoint, migrate, build amd64 and deploy to production
    # A dispatch cannot be pointed at an arbitrary branch and reach production:
    # the only ref that deploys is the one `pnpm promote` fast-forwards.
    if: ${{ github.ref == 'refs/heads/production' }}
    runs-on: ubuntu-latest # ⚠️ Never a larger runner — those are billed even on a public repo.
    timeout-minutes: 45
    # The approval gate and the credential boundary, in one declaration. These
    # twelve exist ONLY in this environment, so no other workflow here can read
    # them even by typo — and because they are pushed rather than fetched,
    # there is no identity behind them that could be talked into handing over
    # anything else.
    #
    # ⚠️ NO `permissions:` BLOCK, deliberately. The file-level `contents: read`
    # is the whole grant. In particular there is no `id-token: write`: that is
    # granted per job, so it would put GitHub's token-minting variables in front
    # of every step including `pnpm install` and the Docker build. See the
    # header, and local-gate.test.ts, which asserts it stays absent.
    environment:
      name: production
      url: https://spiralclass.com
    env:
      FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
      DATABASE_URL: ${{ secrets.DATABASE_URL }}
      DIRECT_URL: ${{ secrets.DIRECT_URL }}
      # Step 1 of the deploy: the pre-migration Neon checkpoint (D-95). It is
      # FAIL-CLOSED — no credential, no checkpoint, no deploy — and that must
      # stay true. A migration is the one thing redeploying the previous image
      # cannot undo.
      NEON_API_KEY: ${{ secrets.NEON_API_KEY }}
      # A SECRET, though it is an identifier rather than a credential, and it
      # was a `vars` entry until 2026-09-10. Three things that made it one are
      # no longer true: the value is not in infra/database/neon/README.md (the
      # publication sweep replaced it with `see neonctl projects list`),
      # fly-deploy.sh does not default it (the same sweep removed the literal,
      # and neon-checkpoint-retention.test.ts now asserts no default), and
      # "readable in a run log" became the argument against rather than for —
      # this repository is public, and the audit that removed the literal said
      # why: it names the exact Neon project holding real student data, which
      # is the reconnaissance surface D-158 exists to remove. `check-leaks.mjs`
      # gates the whole tree on this identifier's shape; a plaintext `vars`
      # entry reaching a public run log walks around that gate.
      #
      # Masked, so a failing deploy prints `***` here. The branch names and the
      # app name in the same log say which deploy it was. See D-163's second
      # addendum.
      NEON_PROJECT_ID: ${{ secrets.NEON_PROJECT_ID }}
      # config/env/production.build.env's `__LOCAL__` values, resolved from the
      # process environment by scripts/env-config.mjs. Baked irreversibly into
      # the client bundle at build time, which is why they are environment-scoped
      # rather than repository-wide — a preview value shipped to production
      # browsers is not a thing you can take back.
      NEXT_PUBLIC_TEACHER_PHOTOS_R2_PUBLIC_URL: ${{ secrets.NEXT_PUBLIC_TEACHER_PHOTOS_R2_PUBLIC_URL }}
      NEXT_PUBLIC_TEACHER_VIDEOS_R2_PUBLIC_URL: ${{ secrets.NEXT_PUBLIC_TEACHER_VIDEOS_R2_PUBLIC_URL }}
      NEXT_PUBLIC_SUPPORT_WHATSAPP: ${{ secrets.NEXT_PUBLIC_SUPPORT_WHATSAPP }}
      NEXT_PUBLIC_SENTRY_DSN: ${{ secrets.NEXT_PUBLIC_SENTRY_DSN }}
      NEXT_PUBLIC_POSTHOG_KEY: ${{ secrets.NEXT_PUBLIC_POSTHOG_KEY }}
      NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY: ${{ secrets.NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY }}
      # The Cloudflare origin address behind livekit.spiralclass.com, read by
      # the probe step's LiveKit certificate check. Never committed: hiding the
      # origin behind Cloudflare IS the protection, and scripts/local/synthetic.sh
      # exits 2 rather than guess it (D-134).
      LIVEKIT_ORIGIN_IP: ${{ secrets.LIVEKIT_ORIGIN_IP }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      # Before the toolchain, because it needs none of it and the whole point
      # is to be the cheapest step in the job.
      - uses: superfly/flyctl-actions/setup-flyctl@ed8efb33836e8b2096c7fd3ba1c8afe303ebbff1 # v1.6

      - name: Refuse before touching anything if this environment is not complete
        # WHY THIS IS FIRST, AND WHY IT IS NOT A SECOND COPY OF THE DEPLOY.
        #
        # scripts/fly-deploy.sh discovers a missing value WHERE IT IS FIRST
        # USED — which for the database URLs is after step 1 has already cut a
        # Neon checkpoint, and for the Fly token after step 2 has already
        # migrated the production database. Failing there is not dangerous, but
        # it is a half-run deploy to reason about instead of a red run that
        # touched nothing.
        #
        # It matters more than it looks because an ABSENT GitHub secret is not
        # an error — `${{ secrets.X }}` for a secret that does not exist is the
        # empty string, silently. A value the sync has not pushed yet, or one
        # renamed on one side only, therefore reaches the deploy looking exactly
        # like a value that is set, and the first symptom without this step
        # would be a migration running against a database URL of "".
        #
        # Every check below is READ-ONLY and derived, never a restatement:
        #   * The build values come from config/env/production.build.env
        #     through the same env-build-args.mjs the deploy calls, so adding a
        #     __LOCAL__ key there is covered here in the same commit. It is
        #     dependency-free (node: builtins only), which is why this can run
        #     before pnpm exists.
        #   * The Fly token is checked with `-c fly.production.toml`, so the app
        #     name is read out of the config exactly as fly-deploy.sh reads it.
        #     There is no app name in this file to keep in sync.
        #   * LIVEKIT_ORIGIN_IP is checked HERE rather than being discovered by
        #     the probe step, because the probe runs after the deploy. Its
        #     absence would turn a good release into a red run at the one moment
        #     nobody wants ambiguity about whether production shipped.
        run: |
          set -euo pipefail
          missing=""
          [ -n "${DATABASE_URL:-}" ] || missing="$missing DATABASE_URL"
          [ -n "${DIRECT_URL:-}" ] || missing="$missing DIRECT_URL"
          [ -n "${NEON_API_KEY:-}" ] || missing="$missing NEON_API_KEY"
          [ -n "${NEON_PROJECT_ID:-}" ] || missing="$missing NEON_PROJECT_ID"
          [ -n "${FLY_API_TOKEN:-}" ] || missing="$missing FLY_API_TOKEN"
          [ -n "${LIVEKIT_ORIGIN_IP:-}" ] || missing="$missing LIVEKIT_ORIGIN_IP"
          if [ -n "$missing" ]; then
            echo "The \`production\` environment is missing:$missing" >&2
            echo "" >&2
            echo "Infisical is the source of truth and syncs these in (D-163). So fix the" >&2
            echo "value THERE and let the sync carry it, rather than setting it by hand" >&2
            echo "here — a hand-set secret is the copy that stops matching." >&2
            echo "⚠️ On the \`production\` environment, never repository-wide." >&2
            exit 1
          fi
          # Empty DATABASE_URL/DIRECT_URL do not fail loudly inside the deploy
          # script — it treats "not both present" as "read them from the
          # Infisical CLI instead", and a runner has no CLI and no login. The
          # failure would therefore arrive as a missing-binary message about a
          # tool this path never intended to use.
          echo "› Every value this deploy reads arrived."

          # Resolves config/env/production.build.env's __LOCAL__ values from the
          # process environment and throws naming every one it cannot satisfy.
          # Output discarded: this is the throw we want, not the values.
          node scripts/env-build-args.mjs production >/dev/null
          echo "› Every __LOCAL__ build value resolves."

          # The one call that proves the token is live rather than merely
          # present. A Fly token that has been revoked or has expired looks
          # exactly like a working one until it is used.
          flyctl status -c fly.production.toml >/dev/null
          echo "› The Fly token can see the production app."

      - uses: pnpm/action-setup@ea17c68df8912ef543352723c149a84f56e3d413 # v6.1.0
        with:
          dest: ~/setup-pnpm

      - name: Read the Node major the app actually ships on
        run: |
          set -euo pipefail
          major="$(sed -n 's/^FROM node:\([0-9][0-9]*\)-slim.*/\1/p' Dockerfile | head -1)"
          if [ -z "$major" ]; then
            echo "Could not read a Node major from the Dockerfile's FROM line." >&2
            exit 1
          fi
          echo "NODE_VERSION=$major" >> "$GITHUB_ENV"

      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm

      - name: Deploy
        # `--gate-already-passed` is the handoff flag scripts/fly-deploy.sh
        # already understands from `pnpm promote`. It is not a bypass: see this
        # file's header for why the gate provably ran before the push that
        # triggered this.
        run: bash scripts/fly-deploy.sh production --gate-already-passed

      - name: Probe what is now live
        # The probes D-129 chained into `pnpm promote`, moved to where the
        # deploy now happens. They exist because the regressions they catch —
        # a route that renders an error, a booking page with nothing
        # purchasable, a checkout with no payment rail — all return 200, so
        # `flyctl deploy` succeeding says nothing about them, and they are
        # introduced at deploy time.
        #
        # Called directly rather than through `pnpm local synthetic`: that
        # wrapper exists to give a hand-run task the run history and the
        # failure notification that Actions used to provide for free (D-129).
        # On Actions, that is what a red job already is.
        run: bash scripts/local/synthetic.sh

      - name: Say what shipped
        if: ${{ always() }}
        env:
          OUTCOME: ${{ job.status }}
        run: |
          set -euo pipefail
          {
            echo "## Production · ${OUTCOME}"
            echo
            echo "| | |"
            echo "| --- | --- |"
            echo "| Commit | \`${GITHUB_SHA}\` |"
            echo "| App | https://spiralclass.com |"
            echo "| Image | \`registry.fly.io/agendaprofe:${GITHUB_SHA}\` |"
            echo
            echo "Neon \`production\` was checkpointed before migrations ran (D-95)."
            echo "Deployed by \`scripts/fly-deploy.sh production\` — the same script"
            echo "the operator runs by hand for a recovery deploy."
            echo
            echo "What is live: \`flyctl status -c fly.production.toml\` — the image"
            echo "tag IS the commit. Not \`pnpm release:status\`: its ledger is"
            echo "per-machine and this deploy did not run on that machine."
          } >> "$GITHUB_STEP_SUMMARY"
